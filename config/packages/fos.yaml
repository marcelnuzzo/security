# App/config/packages/fos.yaml

fos_user:
    db_driver: orm # other valid values are 'mongodb' and 'couchdb'
    
    firewall_name: main
    user_class: App\Entity\User

    from_email:
        address: "nuzzo.marcel@aliceadsl.fr"
        sender_name: "marcel"
        
framework:
    templating:
        engines: ['twig','php']